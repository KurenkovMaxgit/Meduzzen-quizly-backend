# The listening port for the API server
PORT=8080

# The current runtime environment (development / production)
NODE_ENV=test

# The allowed origin(s) for CORS requests ('https://myapp.com' or '*' for public)
CLIENT_URL=*

# Postgres credentials
POSTGRES_HOST=localhost
POSTGRES_PORT=5433
POSTGRES_USER=Hubabuba_test
POSTGRES_PASSWORD=VerY_sTrOng_and_reAdAblE_db_password_for_test_db
POSTGRES_DB=Quizly_hubabuba_test_db

# Redis credentials
REDIS_HOST=localhost
REDIS_PORT=3885
REDIS_PASSWORD=Very_sTrOng_ReddIsss_passsword_for_test_s

#JWT configs
JWT_ACCESS_TOKEN_SECRET=test_access_secret
JWT_ACCESS_TOKEN_EXPIRES_IN=6h
JWT_REFRESH_TOKEN_SECRET=test_refresh_secret
JWT_REFRESH_TOKEN_EXPIRES_IN=7d

#Auth0 config
AUTH0_ISSUER_URL=https://dev-6z6zh67brhm6ovej.us.auth0.com/
AUTH0_AUDIENCE=https://quizly-api.com