services:
  quizly-api-prod:
    build:
      context: .
      dockerfile: Dockerfile.prod
    ports:
      - '8080:8080'
    environment:
      - PORT=8080
      - NODE_MODE=production
    volumes:
      - ./src:/app/src
