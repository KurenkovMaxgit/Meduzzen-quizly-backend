export enum UserRole {
  USER = 'user',
  SUPER_ADMIN = 'super_admin',
}

export enum CompanyStatus {
  VISIBLE = 'visible',
  HIDDEN = 'hidden',
}

export enum CompanyRole {
  OWNER = 'owner',
  ADMIN = 'admin',
  MEMBER = 'member',
}
